export const addTask = (task) => ({
  type: 'ADD_TASK',
  task
});

// const resetTask = () => ({
//   type: 'RESET_TASK'
// });

export const inputTask = (task) => ({
  type: 'INPUT_TASK',
  task
});